<nav class="col-md-2 d-none d-md-block bg-dark sidebar h-100">
  <div class="position-sticky pt-3 h-100">
    <!-- ログインしているユーザーのメールアドレスを表示 -->
    <% if user_signed_in? %>
      <div class="px-3 text-white">
        <p><%= current_user.email %></p>
      </div>
    <% end %>

    <ul class="nav flex-column text-white">
      <li class="nav-item">
        <button id="mypage-link" class="btn btn-link text-decoration-none">マイページ</button>
      </li>
      <li class="nav-item">
        <%= link_to 'ホーム', root_path, class: 'nav-link text-white' %>
      </li>
      <li class="nav-item">
        <%= link_to 'フォルダー一覧', folders_path, class: 'nav-link text-white' %>
      </li>
      <li class="nav-item">
        <%= link_to '新しいメモ', new_folder_memo_path(@folder), class: 'nav-link text-white' %>
      </li>

      <%# メモの新規作成、編集ページ内のみ表示する %>
      <% if controller_name == 'memos' && (action_name == 'new' || action_name == 'edit') %>
        <li class="nav-item">
          <%= link_to 'テンプレート', '#', id: 'template-link', class: 'text-white', data: { bs_toggle: 'modal', bs_target: '#templateModal' } %>
        </li>
      <% end %>

      <li class="nav-item">
        <%= link_to 'ログアウト', destroy_user_session_path,  data: { turbo_method: :delete }, class: 'nav-link text-white' %>
      </li>
    </ul>
  </div>
</nav>
